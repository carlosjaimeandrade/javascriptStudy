const [a, b, c, d] = [3,5,6,1]

const soma = a + b + c + d
const subtracao = d - b
const multiplicação = d * b
const divisao = d / a 
const modulo = a % 2 

console.log (soma, -subtracao, multiplicação, divisao)
console.log (soma, subtracao, multiplicação, divisao)